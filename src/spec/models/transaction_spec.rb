require 'rails_helper'
require 'spec_helper'

RSpec.describe Transaction, type: :model do
  #it { is_expected.to have_db_column(:transable_id).of_type(:integer) }
  #it { is_expected.to have_db_column(:transable_type).of_type(:string) }
  #it { is_expected.to belong_to(:transable) }
end
